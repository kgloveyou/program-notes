https://mp.weixin.qq.com/s/9ixLYlyiqZfC_-BqRYpBig

# 前端如何使用Monorepo搭建项目，面试被问到应该怎么说

原创 小杨爱编程 小杨爱编程

_2025年10月5日 10:29_ _四川_

在小说阅读器中沉浸阅读

## 一、什么是 Monorepo？

**Monorepo（Monolithic Repository）** 是一种将多个项目（比如多个前端应用、组件库、工具库等）存放在**同一个代码仓库**中的开发管理模式。

相比传统每个项目一个 repo 的方式，Monorepo 有如下优势：

* 代码复用方便（比如共享组件、工具函数）
  
* 统一依赖管理（避免重复安装）
  
* 跨项目变更容易（比如修改公共组件后，所有依赖它的应用可以同步更新）
  
* 统一的 CI/CD 流程
  
* 更好的团队协作与代码规范统一
  
* ![图片](https://mmbiz.qpic.cn/mmbiz_png/t6XlVdBsLgRzhGfgpqvWe9yEicXDt6aAxe7yicoR3wGACujkehPC4hCL2liaTSTdSNXozrJwSFwZicmYaM9GdxwOnQ/640?wx_fmt=png&from=appmsg&watermark=1&tp=webp&wxfrom=5&wx_lazy=1#imgIndex=0)
  
  ​    
  
* * *

## 二、前端 Monorepo 的常见方案

目前主流的前端 Monorepo 管理工具包括：

| 工具  | 特点  | 适用场景 |
| :--- | :--- | :--- |
| **pnpm + pnpm-workspace** | 快速、节省磁盘空间，生态逐渐丰富 | 中小型到大型项目 |
| **Lerna + Yarn/NPM Workspaces** | 历史较久，适合管理多个 npm 包，但较重 | 多 package 发布型项目 |
| **Turborepo** | 高性能构建缓存，由 Vercel 推出，适合大型复杂 monorepo | 大型项目，注重构建性能 |
| **Rush** | 微软出品，功能全面但配置较复杂 | 企业级大型 monorepo |
| **Nx** | 功能强大，支持任务调度、可视化依赖图，可渐进式接入 | 中大型，尤其是 TS/React 项目 |

推荐新手或中小团队从 **pnpm + workspaces** 或 **Turborepo / Nx** 开始。

* * *

## 三、以 pnpm + workspaces 为例：手把手搭建一个 Monorepo

### 1\. 初始化 Monorepo 项目

```sh
mkdir my-monorepo
cd my-monorepo
pnpm init
```

### 此时会生成一个  `[package.json](http://package.json)` ，内容大致如下：

```json
{
  "name": "my-monorepo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {},
  "dependencies": {}
}
```

### 2\. 配置 pnpm Workspaces

在根目录的  `[package.json](http://package.json)` 中添加 `workspaces`字段，用来指定子包（packages/apps）所在的目录：

```json
{
  "name": "my-monorepo",
  "private": true, // 私有仓库，防止误发布根目录
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --parallel --filter \"./apps/*\" dev",
    "build": "pnpm --parallel --filter \"./apps/*\" build"
  },
  "devDependencies": {
    "pnpm": "^9.0.0"
  }
}
```

private: true是为了防止不小心把根目录发布到 npm。

* * *

### 3\. 创建子项目目录

推荐目录结构如下：

```
my-monorepo/
├── package.json
├── 
            pnpm-workspace.yaml
             （可选，旧版 pnpm 使用，新版已内置到 package.json 的 workspaces）
├── apps/
│   └── web-app/          # 一个 React/Vue 前端应用
│       ├── package.json
│       ├── src/
│       ├── 
            vite.config.ts
           / 
            webpack.config.js
          │       └── ...
├── packages/
│   └── ui-lib/           # 共享的 UI 组件库
│       ├── package.json
│       ├── src/
│       └── ...
│   └── utils/            # 工具函数库
│       ├── package.json
│       └── ...
```

* * *

### 4\. 创建一个 Web 应用（例如 React + Vite）

进入 `apps/web-app`，创建一个 React 应用：

```bash
mkdir -p apps/web-app
cd apps/web-app
pnpm init
pnpm create vite . --template react-ts
pnpm install
```

其  `[package.json](http://package.json)` 可能如下（注意 name 和依赖）：

![图片](data:image/svg+xml,%3C%3Fxml%20version='1.0'%20encoding='UTF-8'%3F%3E%3Csvg%20width='1px'%20height='1px'%20viewBox='0%200%201%201'%20version='1.1'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:xlink='http://www.w3.org/1999/xlink'%3E%3Ctitle%3E%3C/title%3E%3Cg%20stroke='none'%20stroke-width='1'%20fill='none'%20fill-rule='evenodd'%20fill-opacity='0'%3E%3Cg%20transform='translate%28-249.000000,%20-126.000000%29'%20fill='%23FFFFFF'%3E%3Crect%20x='249'%20y='126'%20width='1'%20height='1'%3E%3C/rect%3E%3C/g%3E%3C/g%3E%3C/svg%3E)

```json
{
  "name": "web-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.0.0",
    "typescript": "^5.0.0",
    "vite": "^4.0.0"
  }
}
```

⚠️ 注意：由于我们在根目录启用了 workspaces，所以 `web-app`的依赖如 `react`可以在根目录统一安装，避免重复。

* * *

### 5\. 创建一个共享组件库（packages/ui-lib）

```bash
mkdir -p packages/ui-lib
cd packages/ui-lib
pnpm init
```

### [package.json](http://package.json) 示例：

```json
{
  "name": "@my-monorepo/ui-lib",
  "version": "1.0.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
```

### 然后在 `src/ [index.ts](http://index.ts)` 中导出一些公共组件：

```tsx
// src/index.ts

export const Button = ({ children }: { children: string }) => {
  return <button>{children}</button>;
};
```

### 实际项目中你可能用 Storybook、Rollup 或 ESModule 打包，这里简化为直接导出 TS/JS。

* * *

### 6\. 在 Web 应用中引用共享组件库

由于我们在同一个 monorepo，可以直接通过 **workspace:** 协议引用本地包：

```json
# 在 apps/web-app/package.json中添加依赖
"dependencies": {
  "react": "^18.2.0",
  "react-dom": "^18.2.0",
  "@my-monorepo/ui-lib": "workspace:*"
}
```

然后就可以在 React 代码中引入：

```tsx
// apps/web-app/src/App.tsx
          
import { Button } from '@my-monorepo/ui-lib';

function App() {
  return (
    <div>
      <h1>Hello Monorepo</h1>
      <Button>Click Me</Button>
    </div>
  );
}
```

注意：要确保 `ui-lib`已经构建（如果有编译步骤），或者使用类似 `vite`支持直接引用 TS 源码的配置。

* * *

### 7\. 根目录统一安装依赖（推荐）

为了节省空间和统一版本，建议在根目录安装公共依赖：

`pnpm add -w typescript eslint prettier`

* `-w`表示安装在根目录（workspace root）
  
* 子项目可以通过 workspace 继承或复用这些依赖版本
  

你也可以使用 `pnpm dlx @pnpm/workspace-tools`等工具进一步优化。

* * *

## 四、进阶功能（可选）

![图片](data:image/svg+xml,%3C%3Fxml%20version='1.0'%20encoding='UTF-8'%3F%3E%3Csvg%20width='1px'%20height='1px'%20viewBox='0%200%201%201'%20version='1.1'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:xlink='http://www.w3.org/1999/xlink'%3E%3Ctitle%3E%3C/title%3E%3Cg%20stroke='none'%20stroke-width='1'%20fill='none'%20fill-rule='evenodd'%20fill-opacity='0'%3E%3Cg%20transform='translate%28-249.000000,%20-126.000000%29'%20fill='%23FFFFFF'%3E%3Crect%20x='249'%20y='126'%20width='1'%20height='1'%3E%3C/rect%3E%3C/g%3E%3C/g%3E%3C/svg%3E)

### 1\. 统一脚本管理

在根  `[package.json](http://package.json)` 中，你可以用 `--filter`来对特定包执行脚本：

```json
"scripts": {
  "dev": "pnpm --parallel --filter './apps/*' dev",
  "build:all": "pnpm --parallel --filter './apps/*' build",
  "build:ui": "pnpm --filter @my-monorepo/ui-lib build"
}
```

运行：

`pnpm dev              # 启动所有 apps 下的 dev 脚本pnpm build:all  # 构建所有应用`

### 2\. 依赖提升 / 共享

通过 workspaces，所有子包都默认可以共享根目录的 node_modules，pnpm 会通过硬链接节省磁盘空间，并避免重复安装。

### 3\. 使用 Turborepo / Nx 做任务编排与缓存

如果你有更复杂的构建、测试、 lint 等任务，可以使用 Turborepo或 Nx做更高级的调度与缓存优化，显著提升大型 monorepo 的构建效率。

* * *

## 五、面试回答思路（如何回答“如何设计前端 Monorepo”）

### ✅ 面试官问题举例：

“你了解 Monorepo 吗？你们是怎么组织多个前端项目的？有实践过吗？”

### 🎯 推荐回答结构：

#### 1\. 什么是 Monorepo？

Monorepo 是一种将多个项目（前端应用、组件库、工具函数等）放在一个代码仓库中进行管理的模式。它有助于代码共享、统一依赖和高效协作。

#### 2\. 为什么要用 Monorepo？（优势）

* **代码复用**：比如多个应用共用一个 UI 组件库或工具函数。
  
* **统一依赖**：避免每个项目都装一遍 react、typescript，节省空间和版本冲突。
  
* **原子性提交与变更**：修改公共库和引用方可以一次提交，便于追踪。
  
* **统一工具链**：ESLint、Prettier、测试工具可以统一配置。
  
* **CI/CD 流程更简单可控**
  

#### 3\. 如何设计一个 Monorepo？（技术选型与实现）

我通常选择 **pnpm + workspaces** 方案，因为：

* pnpm 速度快，且通过硬链接节省磁盘空间；
  
* workspaces 能很好地管理多包结构；
  
* 社区生态逐渐完善，学习成本低。
  

**目录结构设计：**

```
my-monorepo/
├── apps/                 # 前端应用（如 React/Vue 项目）
│   └── web-app/
├── packages/             # 公共库（如 UI 组件、工具函数）
│   └── ui-lib/
├── 
            package.json
                    # 根配置，定义 workspaces
```

**关键点：**

* 根目录  `[package.json](http://package.json)` 中配置 `"workspaces": ["apps/*", "packages/*"]`
  
* 各子包通过 `"name": "@scope/name"`定义 npm 包名
  
* 应用通过 `"dependencies": { "@scope/name": "workspace:*" }`引用本地包
  
* 可以通过 `pnpm --filter`对指定包执行脚本
  
* 推荐将公共依赖安装在根目录，统一版本
  

#### 4\. 进阶优化（可选，加分项）

如果项目更复杂，我会考虑：

* 使用 **Turborepo** 或 **Nx** 做任务编排、依赖可视化与构建缓存，提高构建效率；
  
* 结合 **Storybook** 管理和预览 UI 组件；
  
* 用 **changesets** 或 **lerna** 做版本管理和发布；
  
* 配置统一的 ESLint/Prettier/Commitlint 规范
  

#### 5\. 实践经验（如有）

我曾在\[某个项目/公司\]中使用 pnpm + workspaces 管理多个 React 应用和共享组件库，通过 workspace 协议互相引用，配合 Vite 快速启动，大大提高了代码复用率和团队协作效率。

* * *

## 六、总结

| 能力  | 说明  |
| :--- | :--- |
| Monorepo 概念 | 多项目统一仓库管理，提高复用和协作效率 |
| 推荐工具 | pnpm + workspaces（轻量）、Turborepo/Nx（高性能/复杂场景） |
| 目录结构设计 | apps/ 放应用，packages/ 放公共库，根目录配置 workspaces |
| 关键实现 | workspace 协议引用、统一依赖、根脚本管理、CI/CD 友好 |
| 面试回答要点 | 概念 -> 优势 -> 技术选型 -> 目录设计 -> 脚本管理 -> 优化手段 -> 实践 |

* * *

如果你有具体技术栈（比如 React + TypeScript + Vite，或 Vue + Nuxt），我可以进一步给出针对性更强的模板代码与配置。欢迎在评论区！